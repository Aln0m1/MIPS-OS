文件系统 挑战性任务
任务要求
欢迎选择 2026 文件系统挑战性任务！本挑战性任务的主题是对 MOS 现有的文件系统实现进行修复和改进，在保持原本文件系统和 Shell 功能不受影响的前提下，最终实现磁盘块缓存地址的动态分配。

提交内容
整个挑战性任务一共分为四个子任务，你需要完成全部四个子任务，提交实现代码以及实现报告并通过自动化评测才有机会获得全部分数。每个子任务中有 Thinking 和 Challenge 两种问题：对于 Thinking 问题，你只需要在实现报告中回答即可；对于 Challenge 问题，你需要根据题目要求修改代码，并在实现报告中详细描述你所做的修改，最后通过自动化评测，才能得到相应的分数。代码与报告提交通道将会在后续分别发布。

关于大模型的使用
时代在发展，我们鼓励你借助大模型完成设计和编码，但我们更希望你能够和大模型共同研讨出优雅、严谨的解决方案，并在大模型生成内容之后能够学习大模型的解题思路，最终能够完整表达出整个实现流程。因此，我们禁止使用大模型生成实现报告的任何部分，一旦我们发现实现报告中有明显的大模型生成痕迹，你的分数将会被取消。

其它注意事项
四个子任务是逐步递进的，后续任务应建立在前面任务的修改之上。最终提交的代码需要同时满足四个任务的要求，而不是分别提交四份互不兼容的实现。

自动化评测不仅会检查题目中给出的 Shell 命令，还可能覆盖根目录、子目录、初始镜像文件、运行时创建文件以及缓存地址回收后的再次访问。

任务一
在文件所在的目录中，我们使用文件控制块，即 struct File 结构体，来保存文件的相关信息：


struct File {
    char f_name[MAXNAMELEN]; // filename
    uint32_t f_size;     // file size in bytes
    uint32_t f_type;     // file type
    uint32_t f_direct[NDIRECT];
    uint32_t f_indirect;

    struct File *f_dir; // the pointer to the dir where this file is in, valid only in memory.
    char f_pad[FILE_STRUCT_SIZE - MAXNAMELEN - (3 + NDIRECT) * 4 - sizeof(void *)];
} __attribute__((aligned(4), packed));
其中，我们使用 struct File *f_dir 字段来记录 当前文件所在的目录 的文件控制块的地址。然而，在文件控制块中使用内存中的地址作为字段是存在隐患的，这种记录方式绝对不会出现在真实的操作系统中。

Thinking 1.1 请思考并回答，在真实的操作系统中，如果使用内存中的地址作为文件控制块的字段，会出现哪些问题？为什么我们目前的操作系统不会出现这些问题？

为了解决上述问题，你需要参考 struct File 结构体中的其它字段，例如 uint32_t f_direct[NDIRECT] 和 uint32_t f_indirect ，使用磁盘块编号来记录磁盘块所在的位置。

然而，如果想定位到一个文件控制块的位置，只使用磁盘块编号是不够的。一个磁盘块的大小是 4 KB ，而一个文件控制块的大小是 256 Bytes ，为了能够定位到具体的文件控制块，我们还需要一个范围为 [0, 4096) 的磁盘块内偏移，即可替代掉原来的 struct File *f_dir 字段。

Challenge 1.1 请在 user/include/fs.h 文件中找到 struct File 结构体的定义，并将其中的 f_dir 字段修改为 f_dir_block 和 f_dir_offset 两个字段，分别记录 当前文件所在的目录 的文件控制块 所在的磁盘块编号，以及当前文件所在的目录 的文件控制块 在磁盘块中的偏移。注意维护 f_pad 字段的长度，保证 struct File 结构体大小依然为 256 字节。

Challenge 1.2 在修改完 struct File 结构体后，你需要同步修改所有使用到原 f_dir 字段的代码，保证整个操作系统能够正确运行。提示：你可以使用 grep 命令查找所有使用到原 f_dir 字段的位置。

一些可能有用的提示
f_dir_block 和 f_dir_offset 共同描述的是 当前文件所在的目录 的文件控制块 在磁盘中的位置，不是 当前文件所在的目录 的内容 在磁盘中的位置，更不是 当前文件的文件控制块 在磁盘中的位置。
f_dir_offset 是磁盘块内的字节偏移。完全有可能出现其数值为 0 的情况。在判断文件控制块中的 f_dir_block 和 f_dir_offset 字段是否有效时（即替换原来的 if (f->f_dir) 字段时），你不应当使用 if (f->f_dir_block && f->f_dir_offset) 作为判断条件。
任务二
在 fs/fs.c 的 file_close() 函数中，我们实现了关闭文件的操作：


void file_close(struct File *f) {
    // Flush the file itself. Then unmap all blocks of the file.
    file_flush(f);
    if (f->f_type == FTYPE_REG) {
        ......
        for (int i = 0; i < nblock; i++) {
            ......
            unmap_block(diskbno);
        }
    }
}
这个函数主要分为两个部分：一是调用 file_flush() 函数，将文件内容中所有被置为 DIRTY 的缓存页写回磁盘；二是如果被关闭的文件是普通文件（即不是目录文件），则解除文件内容中 所有磁盘块的缓存地址 到物理页的映射。

我们重点关注第二步，这里为什么要检查被关闭的文件是否为普通文件呢？换句话说，如果被关闭的文件是目录文件，执行第二步会有什么影响呢？

Thinking 2.1 尝试删除 f->f_type == FTYPE_REG 的条件判断，即对于任意文件都执行上述第二步的操作。在删除前后分别使用 make test lab=6_2 && make run 执行启动 Shell ，依次执行 ls.b | cat.b > aaa.txt 和 cat.b aaa.txt 两条命令，观察输出结果有何变化，将输出结果记录在报告中。请自行调试程序，分析删除后程序运行出现问题的原因。

Challenge 2.1 在观察到上述问题并找到原因后，请你修改相关代码，使得目录文件在关闭时，其文件内容中 所有磁盘块的缓存地址 到物理页的映射 能够被正常解除，且映射解除后不会对后续执行造成影响。在你的提交中，你需要上传已删除 f->f_type == FTYPE_REG 条件判断的版本。

一些可能有用的提示
“解除映射”指解除文件系统服务进程中缓存地址到物理页的映射，不是释放对应的磁盘块，也不能修改文件的磁盘块编号。
目录文件的内容由一组 struct File 目录项组成。目录内容所在的磁盘块被解除映射后，任何指向这些目录项的指针都会立即失效。
本任务要求修复映射解除后的引用关系，而不是通过继续保留所有目录缓存来回避问题。
任务三
在进行完上述修改后，我们再来关注 fs/fs.c 中的一段代码：


int dir_lookup(struct File *dir, char *name, struct File **file) {
    ......
    // Step 2: Iterate through all blocks in the directory.
    for (int i = 0; i < nblock; i++) {
        ......
        // Find the target among all 'File's in this block.
        for (struct File *f = files; f < files + FILE2BLK; ++f) {
            // Compare the file name against 'name' using 'strcmp'.
            // If we find the target file, set '*file' to it and set up its 'f_dir'
            // field.
            /* Exercise 5.8: Your code here. (3/3) */
            if (!strcmp(f->f_name, name)) {
                *file = f;
                f->f_dir = dir;
                return 0;
            }
        }
    }

    return -E_NOT_FOUND;
}
可以注意到，在目录中查找到指定文件时，我们将当前目录的地址写入了该文件的文件控制块的 f_dir 字段。当然，这段代码现在已经被改为了对 f_dir_block 和 f_dir_offset 两个字段分别赋值，但实际上依然存在着一些问题。

你或许会有疑惑，我们为什么要在从目录中查找文件的过程中进行这个赋值？理论上来说，在这个文件被创建的时候，我们就应该能够知道该文件所在的目录的位置，并且为其 f_dir 字段（即目前的 f_dir_block 和 f_dir_offset 字段）赋值，而不应该在查找的时候才进行这个操作。

Thinking 3.1 尝试删除 dir_lookup() 函数中对 f_dir_block 和 f_dir_offset 字段的赋值。在删除前后分别使用 make test lab=6_2 && make run 启动 Shell ，依次执行 echo.b abc > aaa.txt 、ls.b 和 cat.b aaa.txt 三条命令，观察输出结果有何变化，将输出结果记录在报告中。请自行调试程序，分析删除后程序运行出现问题的原因。提示：如果把所有命令中的 aaa.txt 换成 bbb.txt ，输出结果会发生什么变化？这两个文件有什么区别？

Challenge 3.1 在观察到上述问题并找到原因后，请你修改相关代码，使得 dir_lookup() 函数仅用来进行查找，不会在查找过程中对文件控制块的字段赋值。在你的提交中，你需要上传已删除 dir_lookup() 函数中对上述字段的赋值的版本。

一些可能有用的提示
文件可能通过两条不同路径被创建：运行 MOS 时，由文件系统服务中的文件创建逻辑动态创建；启动 MOS 前，由 tools/fsformat.c 将宿主机文件和目录写入磁盘镜像。
根目录没有通常意义上的上级目录。请为根目录设计并统一使用一种可验证的约定，例如使用根目录 FCB 自身在超级块中的位置，避免把无效内存地址写入磁盘。
任务四
众所周知，在我们目前的操作系统中，磁盘块在内存中的缓存采用的是一对一的设计，即每个磁盘块对应着内存中一段固定的虚拟地址。然而在现实的操作系统中，内存往往没有足够的空间，能够一对一缓存全部的磁盘块。这就需要我们在将磁盘块读入内存时，为其动态分配用于缓存的地址，并维护一张磁盘块编号到缓存地址的表，用于根据磁盘块编号进行查找。

Thinking 4.1 请自行设计一种检查地址是否已分配，并实现地址分配的方案。需要满足以下三个要求：一是可供分配的地址范围为 [0x10000000, 0x50000000) ；二是保证已分配的地址能够正确被回收；三是保证较低的地址优先被分配。在设计好分配方案后，分析该方案占用的时间和空间，并据此简述为什么选取该方案。提示：你可以回顾本学期实验课程中遇到的所有分配方案，例如页控制块和进程控制块的分配、asid 的分配、fileid 的分配、fdnum 的分配等。

一些可能有用的提示
请明确分配、回收、检查是否已分配三种操作的时间复杂度，以及辅助数据结构的总空间开销。上述三种操作的时间复杂度往往是此消彼长的关系，因此本题没有标准答案，选择一种你认为最优的方案，并能够自圆其说即可。
“较低的地址优先”要求在存在多个空闲页时返回地址最小的页面；回收低地址后，后续分配也应重新优先考虑该地址。
Thinking 4.2 请自行设计一种数据结构，用于存储磁盘块编号到缓存地址的映射，使得时间复杂度最小。注意存储并维护磁盘块编号不对应任何缓存地址的状态（即磁盘块未被缓存的状态）。

一些可能有用的提示
请明确查找、建立映射、删除映射的时间复杂度，并定义“未缓存”状态的表示方式。
映射表记录的是“磁盘块编号到当前缓存虚拟地址”的关系，不是磁盘空闲块位图，也不能替代页表中的有效位检查。
Challenge 4.1 根据你上述的设计，实现磁盘块缓存地址的动态分配功能。

参考实现思路
考虑到任务四相对比较困难，下面给出一条可参考的实现路径，你也可以自行设计修改思路：

实现缓存地址分配器

根据你之前的设计，新增一套管理缓存地址是否已分配的数据结构，负责在 [0x10000000, 0x50000000) 中申请和回收页对齐地址，并保证低地址优先。
你可以在 fs/fs.c 的顶部创建一些辅助函数，例如 alloc_cache_page()、free_cache_page() 等函数。
与之相关的调用点需要同步修改，可能会用到 read_block() map_block() unmap_block() 等函数。
实现将磁盘块编号转换为缓存地址

根据你之前的设计，新增一张维护“磁盘块编号 -> 当前缓存虚拟地址”的映射表，未缓存时应能表示为空。
这张映射表首先会影响 disk_addr() 函数，disk_addr() 不应再按公式计算地址，而应返回当前映射表里的结果；如果某块未缓存，应返回 0 或等价空值。
接下来，在分配或回收缓存地址时，你需要同步修改映射表，严格防止映射表中查找不到表项，或出现过期的表项。
在使用 disk_addr() 查找磁盘块对应的缓存地址时，有可能查找到空值，说明当前磁盘块没有被调入缓存，你需要对这种情况进行检查和处理，可能会用到 block_is_mapped()、read_block()、map_block()、unmap_block()、dirty_block()、write_block()、block_is_dirty() 等函数。
修改位图的访问逻辑

虽然在当前的设计中，位图实际上只占一个磁盘块，但我们在代码中依然保留了位图占据多个磁盘块时的访问逻辑。
目前的访问逻辑是基于位图占据连续的虚拟地址空间这个前提的，但在动态分配缓存地址的情况下，相邻磁盘块的缓存地址却不一定连续，于是我们需要对其访问逻辑进行修改。
可能会用到 free_block() block_is_free() alloc_block_num() 等函数。
检查初始化函数是否成立

fs_init() 调用了 read_super() check_write_block() read_bitmap() 三个初始化函数，其中可能会出现使用硬编码的地址进行启动检查的情况，你需要对这种情况作出调整。
如果这些函数里仍然直接依赖固定地址，启动阶段就会出错，即使后面的普通读写逻辑已经改完。
实现 Open 结构体中 o_file 字段的动态更新

同一磁盘块重新载入后，虚拟地址可能不同，Open 结构体中的 o_file 字段不能继续使用旧指针，你需要在每次调用 serve_*() 函数时，检查并更新其 o_file 字段。
可能会用到 serve_map() serve_set_size() serve_close() serve_dirty() 等函数。
其它可能的遗留问题

上述参考实现思路基于一个假设，那就是你在前三个任务中没有给自己埋雷，否则你还需要解决前三个任务中遗留下来的一些问题。
例如，如果你在前三个任务中使用缓存地址倒推计算磁盘块编号，或者没有调用 disk_addr() 函数而是直接使用硬编码计算缓存地址，诸如此类的操作都可能会对任务四的实现造成影响。
你可能需要重新审视前三个任务的修改内容，并进一步适配动态分配缓存地址的设计。
一些可能有用的提示
实现动态分配后，缓存地址只在对应磁盘块处于缓存状态时有效。同一个磁盘块在两次读入内存时可能获得不同的缓存地址。
不同磁盘块的缓存地址不再由块号计算得到；相邻磁盘块也不保证映射到相邻虚拟地址。
磁盘块未缓存不等于磁盘块处于空闲（即 free ）状态；即使是有内容的磁盘块，也有可能没有缓存到内存中。
任何跨函数或跨请求保存缓存虚拟地址的代码都需要重新检查。磁盘块编号以及块内偏移才是能够在缓存回收后继续使用的稳定信息。